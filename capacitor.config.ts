import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.examen',
  appName: 'examen',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
